import { Category } from "@/components";
import styled from "styled-components";

const Cars = () => {
  return (
    <Container>
      <Category />
    </Container>
  );
};

export default Cars;

const Container = styled.div``;
